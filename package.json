{
  "name": "on-save",
  "version": "2.2.1+baincd",
  "description": "Run a shell command when you save a file.  Forked from mvila's on-save, adding refresh of git status in tree view",
  "keywords": [
    "save",
    "file",
    "run",
    "command",
    "watch",
    "watcher",
    "trigger",
    "hook",
    "track",
    "execute",
    "shell"
  ],
  "repository": "https://github.com/baincd/atom-on-save",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "scripts": {
    "lint": "eslint ."
  },
  "dependencies": {
    "minimatch": "^3.0.4",
    "mkdirp": "^0.5.1"
  },
  "devDependencies": {
    "eslint": "^4.16.0",
    "eslint-config-xo": "^0.19.0"
  }
}
